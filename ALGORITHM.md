# 缠论 60 分钟回测 — 算法文档

> **本文档是算法唯一事实来源(single source of truth)。**
> 任何影响算法行为的修改(技能 SKILL.md、`chan/*.py`、入口脚本)都必须同步更新本文档;
> 只改参数不改逻辑时,也要更新「参数表」。
> 最后更新: 2026-08-17(正式一买严格化：完整相邻下降中枢、强隔离、C创新低、趋势背驰与因果二买链)

---

## 1. 管线总览

```
kline_60min 表 (kline.db)
  ↓ load_60min (confirm_60min.py, 会话缓存)
60min K线 RawBar
  ↓ bars.py: EMA/MACD/macd_area
  ↓ fractal.py: 包含处理 → 无包含K线序列
  ↓ bi.py: 分型 → 笔 (复刻 CZSC, min_bi_len=6)
  ↓ zhongshu.py: 笔中枢 L1 (ZG/ZD, 入口笔定方向)
  ↓ scanner_60min.py: 走势类型归并(同向不重叠中枢→Trend)
  ↓                 L2中枢(3个连续走势类型重叠, 至少1个含≥2中枢的趋势)
  ↓                 买点候选(一买/二买/三买 BuyCandidate, 含有效期窗口)
  ↓ backtest_60min.py: 逐bar行走
  │   结构破坏跟踪(破位作废) → 监控关键区 → 底分型锚定 → MACD力竭确认 → 入场
  ↓   出场: 结构止损 → 10%止损 → 峰值回撤10%移动止盈
交易明细 trades
```

---

## 2. 基础层算法

### 2.1 bars.py — 基础数据结构

- `RawBar`: K线结构(symbol/dt/open/high/low/close/vol/amount/id)
- `ema(data, period)`: 前 period 根取简单平均作为初始值, 之后 `v_t = k·x_t + (1-k)·v_{t-1}`, `k = 2/(period+1)`
- `macd(closes, fast=12, slow=26, signal=9)`: 标准 EMA 差值。返回 `(diff, dea, hist)`, `hist = diff - dea`
- `macd_area(diff, dea, start_idx, end_idx, direction)`: MACD 柱子面积(Ch24 逻辑)
  - `direction='down'`: 只累计 `hist < 0` 的绿柱(负值)
  - `direction='up'`: 只累计 `hist > 0` 的红柱

### 2.2 fractal.py — 包含处理与分型 (Ch62-65)

**包含处理**(`_remove_include`, 复刻 CZSC remove_include):
- 方向只用 **HIGH** 判定: `k1.high < k2.high → up`, `> → down`, 相等则不合并
- 向上: `high = max(k1.high, k2.high)`, `low = max(k1.low, k2.low)`, dt 取贡献更高 high 者
- 向下: `high = min(...)`, `low = min(...)`, dt 取贡献更低 low 者

**分型**(`find_fractals`, 复刻 CZSC check_fx, **HIGH+LOW 双重条件**):
- 顶分型: `b1.high > b0.high 且 b1.high > b2.high 且 b1.low > b0.low 且 b1.low > b2.low`
- 底分型: `b1.low < b0.low 且 b1.low < b2.low 且 b1.high < b0.high 且 b1.high < b2.high`
- 注意: 必须高低点同时满足, 缺一不可(与只用 HIGH 判方向的包含处理不同)

### 2.3 bi.py — 笔 (CZSC 复刻 + 锚点修复, Ch62-65)

> **⚠️ 与 CZSC 原版的关键差异(锚点机制, 2026-08-04 修复)**:
> CZSC 原版成笔后 ubi 截断到终点分型前一根, 若该处恰好是对面类型分型
> (底分型的 left 是顶分型), `fxs[0]` 会漂移, 导致下一笔从中间开始:
> ① 产生连续同向笔; ② 向下笔延伸中反弹超过笔起点时永远成不了反向笔,
> 一根 7 个月大向下笔吞掉 +26% 反弹(60min 级别严重)。
> 修复: 锚点机制(见下), 全市场验证连续同向笔 = 0。

**`_check_bi(bars_ubi, min_bi_len=6, anchor=None)`**:
- **锚点分支**(后续笔): `anchor` = 上一笔终点分型, 作为 `fx_a` 固定锚:
  1. `fx_a = anchor`(价格区间/时间从记录的 `_end_fx` 取, 不依赖已被合并的 bar)
  2. 对面分型必须**在锚点之后**(`f['dt'] > anchor['dt']`)且价格条件:
     - 锚是底 → `fx_b = max(顶分型, key=price)`
     - 锚是顶 → `fx_b = min(底分型, key=price)`
  3. `ab_include` 判定 + `full_bars`(锚点后第一根到 fx_b 后一根)≥ `min_bi_len` → 成笔
- **原版分支**(仅第一笔): `fx_a = fxs[0]`, 对面取最极端分型(等价 CZSC)
- 成笔返回 `bi_data` 附带 `start_fx` / `end_fx`(规范化分型 dict, 供锚点复用)

**`_build_bi(bars)`** — 逐根喂K线, 维护 `anchor`:
1. 逐根做包含处理维护无包含序列 `ubi`
2. **第一笔**: 原版逻辑(同类型分型取最极端作为起点) → `_check_bi`; 成笔后 `anchor = end_fx`
3. **后续笔**: `_check_bi(ubi, min_bi_len, anchor)` 尝试成笔; 成笔后 `anchor = 新笔 end_fx`
4. **笔破坏回退**(保留 CZSC `__update_bi` 后处理):
   - 向上笔破高 / 向下笔破低 → pop + 恢复 ubi, **anchor 回退到被 pop 笔的起点分型** `_start_fx`(延伸后从原起点重新成笔, 终点被延伸更新)

**`build_bi(bars, level)`**: 带增量缓存(`state.py`), 缓存 key = (level, symbol); level ∈ {D, W, 60}

### 2.4 zhongshu.py — 笔中枢 L1 (Ch18/20)

- 从笔序列连续 3 笔开始: `zg = min(三笔high)`, `zd = max(三笔low)`
- `zg <= zd` → 不成中枢, 滑动到下一笔
- **向右扩展**: 后续每笔若 `nzg > nzd`(重叠区不消失)则并入
- `gg = max(全部笔high)`, `dd = min(全部笔low)`, `ZG=zg`, `ZD=zd`
- **中枢方向 = 入口笔方向**(进入重叠区的那笔, 不是重叠区第一笔): 入口笔向下 → '向下'
- 缓存 key = (level, 笔数)

> **⚠️ 扩展逻辑的边界(2026-08-05 审计)**:
> "共同重叠区不消失"扩展会把**大幅突破段**并入延伸(例: 001282
> 2025-03 的 up 24.44→34.36 被并入 02 月小中枢, gg 拉到 34.36)。
> 曾尝试"完全穿越段停止延伸"修复 → 测试副作用过大(L1 42→62、
> 走势 20→43、000591 一买消失) → **已回滚**。
> 结论: ① 突破段的极端点是真实走势高低点, 对 L2 区间计算
> (取 min/max)实际无影响; ② 窄中枢(区间宽度 <2%, 如
> [20.97,20.99])是 8 笔共同重叠的数学产物, 课本字面允许;
> ③ 图上已用虚线+淡色区分窄中枢(plot_chanlun_60min.py)。

### 2.5 state.py — 级别感知缓存

- `_bi_state`: (level, symbol) → 笔增量状态; `_zs_cache`: (level, bi_len) → 中枢列表
- **回测纪律: 逐股切换必须 `clear_all()`**(60min 引擎已内置)

---

## 3. 60min 层算法

### 3.1 scanner_60min.py — L1→走势类型→L2中枢→买点候选

**走势类型归并 `build_trends(zs_list)`**:
- 相邻中枢: 同方向 且 **区间不重叠**(`min(zg)>max(zd)` 为假) → 合并为一个走势
- 方向变或重叠 → 新走势; `n_zs>=2` 记 '趋势', 否则 '盘整'
- Trend: high=GG(全部中枢gg最大), low=DD, start/end 取首末中枢

**L2中枢 `build_l2_zs(trends, min_trend_zs=1)`**(消费式, 2026-08-05 修复):
- 3 个连续走势类型, `zg = min(三走势high)`, `zd = max(three走势low)`
- `zg > zd` → 构成中枢(方向 = 第一个走势方向)
- **消费式**: 3 个走势一旦形成重叠即消费掉(`i += 3`), 下一个中枢从其后开始;
  不成立(`zg <= zd`)才滑动 1 格
- **修复前(滑动窗口)**: `for i in range(len(trends)-2)` 每次窗口 [i,i+1,i+2],
  相邻 L2 共享 2 个走势 → 时间重叠的影子中枢 → "两个连续向下 L2"可能被
  误判为趋势两中枢 → 假一买。000591: 10个重叠 → 修复后 6个依次衔接, 0重叠
- `min_trend_zs=1`: 3 个盘整走势重叠也是有效中枢(000591 2024-10~2025-02
  的 L2[4.42,4.77] 由 3 个单中枢走势构成, 不应被过滤)

**一买候选**(两个连续向下 L2, 2026-08-05 修正 A 价):
- `a, b` 均为向下 且 `b.zg < a.zd`(中枢下移)
- MACD 面积背驰: `abs(area_b) < abs(area_a) * 0.9`(后段绿柱 < 前段×0.9)
- **信号日 = 背驰点实际日期**(第二个向下L2 时间范围内最低bar的日期,
  2026-08-05 修复: 之前用 L2 结束日, 但背驰低点可能更早, 图标注悬空)
  例: 000591 背驰低点 3.97 在 2024-09-18, 不是 L2 结束日 2024-10-08
- 有效窗口 = 104 周(从背驰点日期起算)
- **`a_price = 背驰点实际低点`(不是中枢 ZD!** 例: L2[3].zd=4.52 但低点=3.97)

**三买候选**(向上 L2):
- 向上 L2 完成后, 离开 ZG(`post 中出现 high > zg`)后, 回试低点 > ZG 且现价 > ZG
- 信号日 = 最后一个走势内 dd 最低中枢的结束日(背驰点); 窗口 = 26 周
- 记录 `zs_zg / zs_zd / zs_end_date`

**二买候选**(课本: 日线一买后第一次 60min 回调):
- **买卖点定律一**: 任何级别二买 = 次级别一买。日线二买 = 日线一买后,
  60min 走势序列**第一个向上走势(反弹) → 第一个向下走势(回调)**
- 回调低点(`pullback.low`) > 一买 A 价×0.98(不破一买低点) → 二买确认
- 信号日 = 回调结束日; 窗口 = 26 周
- `a_price = 一买低点`(结构破坏参考), `b_price = 反弹走势 GG`,
  **`c_price = 回调低点`(入场参考), `c_date = 回调走势内最低bar日期`**(图标注用)
- (旧逻辑"三个 L2 down-up-down"等的是日线级别回调, 跨年 14 个月, 不符合课本)

### 3.4 weekly_filter.py — 周线过滤(区间套最外层, 2026-08-05 新增)

- 课本 Ch49: "没必要参与操作级别及以上级别的下跌与超过操作级别的盘整"
- 操作级别=日线 → 周线是上级 → **周线向下时, 日线买点不参与**
- 构建: 周线K线(kline_weekly 表) → 周线笔(level='W') → 周线笔中枢 → 最近中枢方向
- 输出: '向上' / '向下' / '盘整' / '无数据'; 最近中枢结束超12个月则参考倒数第二个
- **时点注意**: weekly_direction 返回**当前状态**(实盘过滤用);
  回测历史交易时点的周线状态需另行实现(逐bar或候选时点快照)
- 接入: scan_60min 候选附 `weekly_dir` 字段; 图上 meta 显示周线方向

### 3.1b 图标注定位规则(2026-08-05)

- **一买**: 图上 ◇ 标注在 `a_date`(背驰点实际日期), 不是信号日(L2结束日)
- **二买**: 图上 ◇ 标注在 `c_date`(回调走势内最低bar日期)
- **实际交易**: 绿▲=入场(日期+价格), 红▼=出场(日期+价格+盈亏), 来自回测结果 json
- **性能**: 回测入口 get_symbols 必须 SQL LIMIT 下推(4.7G 库全表 DISTINCT 会超时)

### 3.2 confirm_60min.py — 数据加载与次级别确认

**`load_60min(symbol)`**: 读 `kline_60min` 表, 会话缓存 `_60MIN_CACHE` / `_CONFIRM_CACHE`(回测逐股必须 `clear_60min_cache()`)

**确认函数**(日线定方向时用, 60min 确认入场; 当前 backtest_60min 主线未调用, 保留备用):
- `confirm_sanmai_60min`: 价 > ZG; 近 240 根 bar 低点距 ZG ∈ (-10%, +30%); 近 60bar 高点/低点 > 1.05; 底分型(近15根内且在近期低点 3% 内) + 至少 1 个辅助信号(绿柱缩小≤0.75 / DIFF金叉 / 缩量≤0.8)
- `confirm_ermai_60min`: 不破 A; 最近向下笔不破 A; 与前向下笔 MACD 面积比 < 0.9 或 DIFF 拐头或底分型
- `confirm_yimai_60min`: 价格在 A ±8% 内; 60min 底分型 + DIFF 连续 2 根拐头; 可选 60min 双下移中枢背驰
- `confirm_zhenmai_60min`: 价格在 ZD 的 -8%~+2% 内; 绿柱缩小(近4根 < 前4根×0.8)或底分型

### 3.3 backtest_60min.py — 逐bar回测(主线)

**候选过滤**:
- 信号有效期: 一买 104 周 / 二买 26 周 / 三买 26 周; 回测入口再截断为 `SIGNAL_WINDOW_WEEKS = 13`(3个月不进关键区则过期)

**结构破坏跟踪(课本: 破位即作废)**:
- 二买: 信号后任何 bar `low < A×0.995` → C 破 A, 候选作废
- 三买: L2 完成后(wait_leave)离开 ZG → pullback 阶段; 回试 `low < ZG×0.98` → 作废

**入场条件(三种买点统一套路: 关键区 + 底分型锚定 + 力竭确认)**:
1. 价格在关键区(监控模式激活):
   - 一买: `cur ∈ [A×0.92, A×1.12]`
   - 二买: `cur ∈ [C×0.98, C×1.05]`(C = 回调低点, 2026-08-05 起; 旧为 A 区)
   - 三买: `cur ∈ (ZG, ZG×1.05]` 且已完成 离开→回试 结构
2. 底分型锚定: 最近底分型中间 bar 在最后 6 根内; 分型低点落在关键区(二买 A×0.98~1.05 / 三买 ZG×0.98~1.05); 当前 bar 未破分型低点
3. 力竭确认 `_divergence_at_fractal`: 以底分型为锚, 下跌段起点 = 分型前 40 根内摆动高点(近端回踩高点, 非数月前高点); 下跌段 ≥ 20 根 bar; 后 1/3 绿柱面积 < 前 1/3 × 0.6; 近 5 根 DIFF 拐头
4. 辅助: 一买还需 `_is_recovering`(近5根收盘 > 起点低点)
5. 优先级: 三买 > 二买 > 一买; 同笔入场后候选标记 consumed
6. 退出后 `ENTRY_GAP = 20` 根 bar 内不再次入场

**出场逻辑**:
1. 结构止损: 持仓标记 `a_low` → `cur < A×0.995`; 或 `entry_zg` → `cur < ZG×0.97`
2. 硬止损: 浮亏 > 10%
3. 移动止盈: 峰值 > 入场价 ×1.05 后, `cur < peak×0.90` → 止盈
4. 回测结束: 未平仓按最后 bar 收盘结算, reason='回测结束'

---

## 4. 参数表(改参必改此表)

| 参数 | 值 | 位置 |
|---|---|---|
| min_bi_len(成笔最少K线) | 6 | bi.py `_check_bi` 默认参 |
| MACD 参数 | 12/26/9 | bars.py `macd` |
| 二买 C 区间 | cur ∈ [C×0.98, C×1.05](C=回调低点), 分型低点 C×0.98~1.05; 破 A(一买低点)×0.995 作废 | backtest_60min |
| 一买 A 区间 | cur ∈ [A×0.92, A×1.12] | backtest_60min |
| 三买 ZG 区间 | cur ∈ (ZG, ZG×1.05], 分型低点 ZG×0.98~1.05 | backtest_60min |
| 底分型最近确认 | 分型中 bar 距当前 ≤ 6 根 | backtest_60min |
| 力竭面积比 | 后1/3 < 前1/3 × 0.6 | backtest_60min |
| 背驰面积比(候选) | 后段 < 前段 × 0.9 | scanner_60min |
| 二买 C 范围(2026-08-17) | c_low ∈ [a_low×0.90, a_low×1.08]；回调必须在正式一买首次可见后开始并确认 | backtest_daily_30min |
| 结构破坏阈值 | 二买破 A×0.995; 三买破 ZG×0.98 | backtest_60min |
| 信号有效期(2026-08-11) | 一买中阴边界 BUY1_WINDOW_WEEKS=26; 二/三买入场窗口 SIGNAL_WINDOW_WEEKS=26 | backtest_daily_30min |
| 二买入场比上限(2026-08-17) | 仅接受正式趋势背驰一买来源，执行价≤A×1.10；盘整背驰不得派生正式二买 | backtest_daily_30min |
| 三卖中枢相关性(2026-08-13) | 仅"持仓峰值≥中枢ZG"的向上中枢可构成日K三卖(peak30≥ZG, §7.15 F1修复) | backtest_daily_30min |
| 出场(2026-08-11 卖点体系) | 日K一卖(向上笔+红柱面积背驰后1/3<前1/3×0.9+新高); 日K三卖(破持仓期向上中枢ZD×0.97); 结构止损 A×0.995/ZG×0.97; 硬止损 10%。移动止盈已删除 | backtest_daily_30min |
| 入场间隔 | 退出后 20 根 bar | backtest_60min |
| 下跌段最小长度 | ≥ 20 根 bar | backtest_60min |

---

## 5. 算法来源

| 模块 | 来源 | 说明 |
|---|---|---|
| fractal | **复刻 CZSC (zengbin93/czsc)** | 包含处理/分型逐行对齐 |
| bi | **CZSC 复刻 + 锚点修复** | 原版 check_bi/__update_bi 为主干; 2026-08-04 加锚点机制修复 60min 起点漂移/反弹被吞(见 §2.3) |
| zhongshu | CZSC `_zs_250705` 对照 | 自实现, 入口笔定方向 |
| segment(已删除) | ~~移植 0xSchnappi/chanlun~~ | 完整移植 Combiner+Seg 管线; 60min 管线不需要, 已移除 |
| bars.py | 对照 CZSC + talib | EMA/MACD 手写 |

**可借鉴的 0xSchnappi 补丁点**(部分已采用):
- ~~`end_is_peak`: 笔终点必须是峰值~~ → 已通过锚点机制等价实现(anchor 固定为上一笔终点分型)
- `check_fx_valid`: 分型有效性校验(未采用)
- gap 跳空计入成笔跨度(未采用)
- 虚笔机制(is_sure): 尾部未确认笔标记(未采用)

---

## 6. 变更同步规则

1. **本文档是唯一事实来源**: ALGORITHM.md 与代码冲突时, 以代码实际行为为准, 但**必须修正文档**
2. 修改 `chan/*.py` 中任一算法(分型/笔/中枢/走势/L2/买点/入场/出场) → 同步更新对应章节 + 参数表
3. 修改技能 `chanlun-backtest/SKILL.md` 中的管线描述/买点定义/用法 → 检查是否与本文档一致
4. 新增算法模块 → 在本文档新增章节
5. 删除算法模块 → 在本文档标注移除(保留记录, 不覆盖)

---

## 7. 2026-08-06 日K+30min 引擎修改记录(Phase 1)

> 新增引擎 `backtest_daily_30min.py`(日K找买点 + 30min介入, 区间套) + `backtest_daily.py` 修改。

### 7.1 日K层 scan_daily 修改
- **二买移除**: 课本 p37 "二买=一买后第一次次级别回调", 操作级别=日K时次级别=30min
  → 二买由30min介入层确认(原日K层生成产生重复信号)
- **窄中枢过滤 2%→0.3%**: 2%误杀低价股真实窄中枢(000537 一买中枢对 0.5%/1.4%)
- **一买判据增加 `b.dd < a.dd`(背驰点创新低)**:
  课本 p37 趋势定义(低点递减) + p37课 c必须创新低 + 61课"背驰没有创新高/新低不存在"
  → 盘整背驰(不创新低)不再生成一买候选(000591全部盘整背驰被滤除)

### 7.2 30min介入层(backtest_daily_30min.py)
- 候选: 日K一买/三买(scan_daily) + 二买(日K一买后第一次30min回调不破A, 课本买卖点定律一)
- 入场: 关键区 + 30min底分型(6根内) + 30min力竭确认(MACD面积比, 61课"围绕中枢两段比较")
- 出场: 与60min引擎一致(结构止损/10%/移动止盈10%), ENTRY_GAP=40根30min bar

### 7.3 Phase 1 修正(2026-08-06)
| 修改 | 逻辑 | 课本依据 |
|---|---|---|
| 一买破位作废 | bar.low < A×0.995 → 候选dead | 37/53课 结构破坏即作废 |
| 新信号作废旧信号 | 同类型候选只保留最新signal_date | 49课 买点对应"当下最近"结构 |
| A区收窄 | 一买 0.92~1.12 → 0.95~1.08 | 实战: 远离背驰点追高安全边际差 |

### 7.4 5股基准测试结果(Phase 1 后)
6笔→4笔 | 胜率67%→75% | 平均+13.23%→+20.85% | 最大亏-9.4%→-6.6%
000537一买+39.1% | 000539三买+45.6% | 000423二买+5.3% | 000423一买-6.6%

### 7.5 已知边界(待Phase 2/3)
- 中枢扩展仍将突破段并入(20课偏差, ITERATION_LOG已记录, 回滚过)
- 一买未区分趋势背驰/盘整背驰(27课: 盘整背驰仅大级别低位有意义)
- 三买离开/回试为日K线级别近似(53课要求次级别走势)
- 60min引擎未同步Phase 1修改(待做)

### 7.6 Phase 1 同步 + Phase 2 标注(2026-08-06)
- **60min引擎同步Phase1三项**(一买破位作废/新信号优先/A区0.95~1.08): 5笔→3笔(噪音交易被滤)
- **Phase2 一买背驰类型标注**: BuyCandidate.div_type = '趋势背驰'(b.GG<a.DD, 20课中心定理二) / '盘整背驰'
- **标注结果(200只, 2026)**: 22笔一买 **全部=盘整背驰, 趋势背驰=0笔**
  - 根源: 中枢扩展逻辑把突破段并入(GG虚高) → 后GG<前DD永假
  - 27课"盘整背驰低位才有意义"验证: 低位21笔33%+0.76% vs 中高位1笔+8.2% → 无区分度(一买天然低位)
- **结论**: 严格"趋势背驰一买"依赖 Phase3 中枢扩展修复(此前回滚过, 需隔离实验)

### 7.7 前视偏差修复(2026-08-06, 重要)
- **BUG**: scan_daily/scan_60min/bar walk 的 edt 参数未截断数据
  → 历史回测(2024/2025)用未来结构生成候选(前视偏差)
- **修复**: 4处扫描 + 3处bar walk 全部按edt截断
  (load_daily/load_60min/load_30min 保持全量缓存, 截断在调用方)
- **修复后跨年验证**(200只, 日K买点+30min介入+Phase1):
  - 2024: 36笔 50% +3.90% | 已平仓 26笔 42% +4.47%
  - 2025: 45笔 62% +10.83% | 已平仓 40笔 65% +12.19%
  - 2026(7个月): 27笔 41% +2.68% | 已平仓 20笔 25% +1.46%
- **结论**: 引擎跨年稳健(三年已平仓全正); 修复前2024"81笔+5.99%"为假象
- 买点类型年度轮动: 2024一买强/2025三买极强(21笔+21.70%)/2026三买强

### 7.8 Phase 4: L2背景过滤(2026-08-06, 已合并默认开启)
- **逻辑**: 候选附 l2_dir(最近日K-L2中枢方向=周线级, 49课"不参与操作级别及以上级别的下跌")
- **过滤**: L2向下时滤一买/二买, 三买豁免(顺势离开结构, 同60min引擎FILTER_WEEKLY)
- **跨年验证**(200只, 已平仓): 关 4.47/12.19/1.46 → 开 4.86/12.56/7.02 (2024/2025/2026)
- **结论**: 三年全部改善 → 默认开启(FILTER_L2=True)

### 7.9 二买起点修正(2026-08-06, 课本p246)
- **BUG**: 二买从 signal_date(中枢完成日)起算找"第一次次级别回调"
  → 信号日滞后背驰点1-3.5个月, 回调识别错位(000400: C=27.80应为23.94)
- **课本依据**: p246"第一类买卖点就是背驰点"; p265"一买次级别上涨后第一次次级别回调"
- **修复**: 二买起点 = a_date(背驰点日期), 两处(scan_30min_entry + scan_60min)
- **效果**(200只已平仓): 2024 +4.86→+5.21 | 2025 +12.56→+12.79 | 2026 +7.02→+10.64
- **二买本身仍偏弱**(缠论特性, p265"二买后常演化大级别盘整"):
  2024: 14笔-3.02% | 2025: 12笔-0.22% | 2026: 1笔+5.29%

### 7.10 架构清理: 删除纯日K引擎(2026-08-07)
- **背景**: 代码库曾有3套引擎:
  1. backtest_daily.py (纯日K, 最早"日K vs 60min"对比产物, 未同步Phase1-4修改链)
  2. backtest_daily_30min.py (日K找买点+30min介入 = 区间套, 主线)
  3. chan/backtest_60min.py (60min, 对比组)
- **决定**: 删除纯日K引擎, 保留两套(区间套主线 + 60min对比组)
- **基础设施提取**: 日K扫描函数(load_daily/clear_daily_cache/scan_daily)
  移到 **chan/daily_scan.py** (被30min引擎+画图脚本共用, 不属于"引擎")
- **引用更新**: backtest_daily_30min.py + chan/analysis/*.py 改
  `from chan.daily_scan import ...`
- **验证**: 两套引擎导入OK; 冒烟测试 000537.SZ 2026 一买 +39.11% 与历史一致
- **遗留**: 纯日K产物(HTML/JSON)已删除, 之后所有图用30min引擎重画

### 7.11 Phase5b: 课本修正版(2026-08-07, 24/27/37/101课)
- **背景**: Phase5严格版(候选b验证+c段创新低)把一买杀光(0笔)——
  判据基准错误(中枢区间vs A/C段) + 盘整背驰天然不满足创新低 + 日K趋势背驰真实罕见
- **修改**(对照课本):
  1. scan_daily面积基准: 中枢区间 → **A段(a前60根摆动高点→a.start) vs C段(b.end后离开段)**；
     C段最迟在后续第一个同级中枢开始前截止，禁止跨过后续中枢回绑更晚低点
     (24课标准, 15课平均力度=面积/时间)
  2. 中枢对: zs_L1相邻 → **相邻向下中枢(中间可隔向上中枢, 20课逐级下移)**
  3. 两类候选: 趋势背驰(c创新低, A=c段低点) + 盘整背驰(c未创新低, A=b.dd时点稳定,
     27课低位由入场层把关); sig=背驰点日(101课), 窗口104→52周
  4. 入场验证v6(_daily_c_divergence): 当下最后向下中枢 + **三卖检查**(回抽<ZG×0.98
     不买, 24/27课"躲过盘整背驰→三卖") + 平均力度背驰 + 盘整背驰低位(3年低×1.08)
     + c段长度≤200根; A区用**当下c低**(防候选A随edt漂移的前视偏差)
  5. 30min区间套: 2个向下中枢 → **最后1个向下30min中枢+离开段背驰**(27课
     "次级别背驰段", 解决入场时点第二中枢未形成的时序矛盾)
- **跨年验证**(200只, 全部口径):
  - 2024: 13笔 69% +9.52% (一买1, 二买2笔+6.40%, 三买10笔+11.35%)
  - 2025: 26笔 81% +18.34% (一买1笔+19.29%, 三买22笔+21.20%)
  - 2026: 2笔 100% +23.26% (三买+45.59%)
- **对比**: Phase5严格版 2024 +8.54% / 2025 +20.15% → 2024改善, 交易类型更完整
- **结论**: 日K趋势背驰真实罕见(200只/年≈1笔, 27课"第二中枢后背驰90%以上"在日K
  级别适用性有限), 三买为主力(101课中枢破坏点); 失败案例(000838/000017三卖)被滤,
  旧信号(000537 2026-01)被52周窗口滤
- **遗留**: 静态候选架构(edt末日扫描)对"历史信号回测"有前视污染(000423盘整背驰
  信号被2026新低覆盖), 已用"入场A区用当下c低"缓解; 彻底方案=入场时动态重扫

### 7.12 Phase5b修复: 进入段误杀 + bi缓存前视(2026-08-07)
- **漏斗诊断**(200只): 一买候选231~259个/年, 但入场仅1笔(0.4%)——瓶颈在验证层
- **修复1(bi.py缓存前视BUG, 严重)**: build_bi增量缓存条件 `n-st['last_len']<3`
  在**截断**(n<last_len)时也命中 → 返回含未来数据的笔 → v6验证出现
  "2024-03-29时点看到2024-08-08的中枢"(000039) → 改为 `0<=n-st['last_len']<3`
- **修复2(进入段误杀)**: v6进入段用"prev.end→b.start"(中枢天然相接=0根) →
  改用"b.start前60根摆动高点→b.start"(与scan_daily A段一致);
  37课"b是有可能小于次级别的,力度最大的就是连续的缺口"→b段短合法
- **效果**(200只2024漏斗): 日K验证通过率 7% → 27% (进入段误杀消失, 前视消失)
- **500只2026对比**:
  - Phase4宽松: 31笔 45% +3.34%
  - 修复版: **8笔 88% +11.34%** (一买2笔+5.89%/二买3笔+0.98%/三买3笔+25.34%)
- **剩余过滤均合理**(课本): 非低位(27课盘整背驰)/三卖(24/27课)/面积未背驰(24课)
- **遗留**: 静态候选时点污染(000423盘整背驰信号被2026新低覆盖) → 彻底方案=
  入场时动态重扫scan_daily(edt=入场日)

---

## 7.13 课本对齐修复(2026-08-11, 二买粒度/中阴边界/卖点体系)

> 背景: 2680只×3年全量回测胜率仅33%, 2026年8笔二买7亏。对照课本原文
> (101课答疑/29课/买卖点定律/16课短差程序) 定位三个代码缺陷 + 一个遗留严重bug。

### 7.13.1 日K游标喂入BUG(严重, Phase R遗留)
- **现象**: `_daily_top_divergence`/三卖检查从未触发; d_cursor.bis 停滞在 sdt 前
- **根因**: 日K喂入代码在 `if day_str != last_scan_day:` 块内, 每天10:00首根
  30min bar 进入块并设置 last_scan_day, 但喂入条件 `bar.dt.time()>=15:00` 在
  15:00 才满足 → 15:00 时 day_str==last_scan_day 块已跳过 → **日K永不喂入**
- **影响**: 动态候选用陈旧笔(sdt前), 卖点检查(依赖d_cursor.bis增长)全失效;
  Phase R 初期验证未覆盖此集成路径(只验证了 BiCursor 类等价性)
- **修复**: 喂入独立为 `if bar.dt.time()>=15:00 and last_daily_updated!=day_str`
- **效果**: 5只测试股 003002 +116% / 002583 +201% / 002015 +49%(修复前无卖出信号)

### 7.13.2 二买定义修正(101课, 笔级次级别回调)
- **课本原文**(101课答疑): "所谓第二类买点, 就是第一类买点的次级别回抽结束后
  再次探底或回试的那个**次级别走势的结束点**...回头肯定有一个1分钟的向下走势,
  这走势的结束点, 就是第二类买点" — **次级别走势结束点, 不要求形成次级别中枢**
- **旧实现(trend版)**: 等30min中枢+背驰 → 错过一买后紧接的浅回调, 信号延迟
  60-229天(002889: 正宗二买07-11@14.64被跳过, 09-16@15.55追高, 3连亏-15%)
- **新实现(笔级)**: 一买后第一个 C/A∈[0.85,1.15] 的30min向下笔, 其 confirm_at
  结束点 = 二买(sig); 前几次回调离A太远(追高)/深破(结构弱)自动跳过
  (003002: 4-6月C/A=1.16~1.36跳过, 7月1.04✓)
- **C区持续更新**(101课"中阴状态内震荡买点也可看作二买"): 窗口内出现更新的
  合格回调 → 更新候选的 c_price(002583: 首C=3.57价格未回踩, 6月新回调C=3.83
  更新后入场 +201%)
- **C/A下限0.85允许浅破一买**(101课最弱情况"二买跌破第一类买点, 完全可以,
  一般都构成盘整背驰"; 深破15%不接; 破位由介入层力竭检查把关)
- **删除21课"向上中枢ZD"约束**: 该约束针对trend深回调, 笔级回调由C/A覆盖

### 7.13.3 一买中阴边界(101课)
- **课本原文**: "只有在这回升的中阴状态下才有第一、二类买点, 中阴状态结束后,
  所有的中枢震荡只存在第三类买卖点...就不存在第一、二类买卖点了"
- **旧实现**: 一买window 52周且二买生成时不检查window → 229天旧一买反复生成
  二买(002889用同一A入3次)
- **新实现**: `BUY1_WINDOW_WEEKS=26`(一买后半年内有效, 覆盖"先涨后回"形态:
  003002一买2月首合格回调7月); 二买生成`ym_active`检查`day_str<=window_end`
- **防重复**: 笔级版"首个合格回调"只生成一次(同一sig), 同源重复自动消除;
  10-27/02-03类二次回调不再入场

### 7.13.4 卖点体系(101课/16课, 替代固定回撤)
- **课本原文**: "第一类买卖点就是背驰点, 第三类买卖点就是中枢破坏点"(101课);
  "大级别买点介入的, 在次级别第一类卖点出现时, 可以先减仓"(16课短差程序)
- **旧实现**: 峰值回撤10%移动止盈 → 002889浮盈+11.4%回吐成-1.7%(出局规则缺陷)
- **新实现**(清仓用日K级别, 与操作级别对称):
  1. **日K一卖**: 持仓期新确认日K向上笔 + 红柱面积背驰(后1/3<前1/3×0.9)
     + 笔终点创新高 → `_daily_top_divergence`
  2. **日K三卖**: 收盘跌破持仓期最后向上日K中枢 ZD×0.97(破中枢不回简化版;
     严格"回抽不回"待增强)
  3. 结构止损(破A×0.995/破ZG×0.97)保留 = 结构破坏退出
  4. -10%硬止损兜底
- **30min一卖不用于清仓**: 次级别背驰可为假背驰(被大级别吞没, 003002 08-01
  30min一卖后继续翻倍) → 仅作短差信号(16课), 本引擎无仓位管理暂不启用
  (`_m30_top_divergence` 保留备用)
- **二卖待增强**: 一卖后次级别反弹不创新高的结束点(后续)

### 7.13.5 参数变更
| 参数 | 旧值 | 新值 |
|---|---|---|
| SIGNAL_WINDOW_WEEKS | 13 | 26(入场窗口, 笔级二买无重复风险) |
| BUY1_WINDOW_WEEKS | - | 26(一买中阴边界, 新增) |
| 二买 C/A 范围 | ≥0.98(不破A) | [0.85, 1.15](允许浅破一买) |
| 移动止盈 | 峰值回撤10% | 删除(由日K一卖/三卖取代) |

### 7.13.6 验证
- 5只测试股(FILTER_L2=False, 2024-2026): 二买7笔 57%胜率 avg+54.66%
  (002583 +201.57% / 003002 +116.40% / 002015 +49.79% / 003030 +38.82%)
- 002889案例: 2025-07-04正宗二买(改动前09-16追高), 同源重复消失
- 200只×3年对比: 见 results/bt_d30_200_3y_v2.json(改动前后笔数/胜率/盈亏)
- 因果: 二买sig=向下笔confirm_at(终点分型第3根bar)≤扫描时刻; 一卖检查在
  日K笔确认后(15:00); 无未来数据

## 7.14 P0 卖点体系课本对齐(2026-08-11, 89课中阴/16课短差/2007-11-20二卖专文)

背景: 对照课本原文(16课茅台案例/17课"下跌+上涨"法/89课中阴/101课/2007-11-20二卖专文)后确认:
- **破A×0.995直接止损非课本做法**: 89课点名批评"抄了底被中阴震出来"; 101课"最弱二买跌破一买完全可以"(跌破=二买机会非卖点)。000012#1 差0.006触发止损后甩下+33%
- **课本退出=次级别(30min)一卖**(16课短差"次级别卖点减仓"/17课"盘整后坚决退出(次级别一卖)")
- **二卖=不能创新高或背驰**(2007-11-20专文), 000592 顶12.46<12.85(标准二卖)全系统无卖点→-41%
- **中阴结束=次级别中枢被第三类买卖点破坏**(90课), 26周时间窗口为近似

改动(backtest_daily_30min.py + chan/bi.py):

### 7.14.1 `_check_bi` 死锁修复(chan/bi.py)
- 原: 锚点后候选取"最极端分型"(最高顶/最低底), 笔长不足(<min_bi_len)时永久死锁,
  直到tail=30窗口把该分型挤出才巧合成笔 → 卖点确认延迟10-12天(000066: 05-27顶06-10才确认)
- 改: 从最极端分型起按序降级, 取**第一个满足笔长**的候选
  (62课本义: "顶和底之间的其他波动都可以忽略不算" → 笔内更高点只是笔内high)
- 000066验证: 04-10顶17.30(4根<6)降级→05-07顶15.40(成笔, h=17.30保留) ✓
- 副作用: 日K笔数76→82(000066), 周线L2方向可能翻转(2024-02-05 向上→向下) → FILTER_L2过滤面变化, 需回归评估

### 7.14.2 卖出体系重构(backtest_daily_30min.py 退出块)
优先级(每根bar收盘判断, t+1成交):
1. **30min一卖(顶背驰)**: 16课短差"次级别卖点" — 条件: 浮盈≥3% + 持仓>60bar(7.5天)
   (浮亏/微利/持仓初期禁用, 避免震荡中被反复割)
2. **30min二卖(不创新高, 低≥1%)**: 同上条件
3. **日K一卖(背驰)**: 101课"一卖=背驰点" — 持仓>160bar(20交易日)才检查;
   末笔down时检查前一支up笔(P0修复000592型窗口错过)
4. **日K三卖(破持仓期向上中枢ZD×0.97)**: 101课"三卖=中枢破坏点" — 保留简化版
5. **破A预警+次级别回抽确认**: 破A×0.995不立即卖(89课不被震出):
   - 30min反弹顶背驰/不创新高 → 确认卖出("破A确认(次级别卖点)")
   - 收复A(收盘) → 解除预警(假破)
   - 10个交易日无像样反弹且新低 → 直接卖出("破A确认(10天新低)")
6. **破三买ZG×0.97**: 29课三买失败 — 保留
7. **止损10%兜底**: 工程保险(防单边暴跌, 非课本)

### 7.14.3 30min短差回补(16课"次级别一买回补")
- 30min一卖/二卖退出后进入回补观察(rounds<3, 60交易日超时):
  回落≥3% + 30min底背驰(`_m30_bottom_divergence`新增, 对称顶背驰) → 回补
- 涨回5%未回补 → 放弃观察
- 2026-08-11冒烟测试发现: 全仓短差在趋势股上破坏大(000592高抛踏空+400%、
  000062回补被套) → 回补机制待回归数据后评估是否保留/收紧

### 7.14.4 背驰短段判据(P0修复)
- 原: 段<10根日K(或<20根30min)时前1/3无红柱 → "无红柱段"必失败
  (000592顶部04-02/05-13两次漏判, -41%无卖点)
- 改: 短段时与**前一相邻同向笔**比较力度(16课"红柱没创新高"判据的笔间版)
- 日K一卖/30min一卖/30min底背驰(回补)均加

### 7.14.5 参数
| 参数 | 值 | 说明 |
|---|---|---|
| 30min卖点浮盈门槛 | 3% | 16课"减仓"的全仓近似(止盈型) |
| 30min卖点持仓门槛 | 60bar | 7.5天, 避免入场即卖 |
| 日K一卖持仓门槛 | 160bar | 20交易日, 避免入场结构延续误判 |
| 30min二卖不创新高 | <peak30×0.99 | 低≥1%才算 |
| 破A预警 | 10天新低 或 次级别卖点确认 | 89课"不被震出" |

### 7.14.6 验证(待200只回归)
- 对比 results/bt_d30_200_3y_v2.json (214笔 36% +5.88%) vs bt_d30_200_3y_p0.json
- 重点观察: ①胜率(破A假破转正) ②利润保持(000592/000066型) ③短差贡献/破坏
- 因果: 所有新卖点均基于已确认笔(confirm_at)收盘判断, t+1成交, 无前视

### 7.14.7 P0二三轮修正(2026-08-11 冒烟测试驱动)
- **30min短差默认关闭**(USE_30M_SWING=False): 16课"可以减仓"是可选增强, 全仓清仓式
  短差破坏趋势股(000592高抛踏空+400%, 000062回补被套-10%)。课本主框架=买点介入→持有→
  卖点出现才走(茅台案例"继续持有直到卖点出现")
- **二卖须前置一卖确认**(101课"二卖=一卖后的次级别反抽结束点"): 前一up笔通过
  `_daily_top_divergence`(长段背驰)才允许二卖(不创新高≥2%+前高≥20交易日)。
  无前置时日K笔级"不创新高"太频繁(000066 8.85<9.49中阴震荡/000062 9.25<9.73中继被误杀)
- **短段(<12根日K)up笔不检查一卖**: 与前笔比力度在趋势中必然假背驰
  (000592 2025-01 +106%被卖飞+800%)。短段顶(000592 12.85/12.46)由三卖/破位兜底
- **破A预警**: 30min向下笔确认结束=最弱二买(101课"跌破一买构成盘整背驰")→持有;
  30min反弹未收复A且顶背驰/不创新高→真破卖出; 收复A→假破解除; 10天新低→卖出
- **FILTER_L2副作用**: 死锁修复改变日K笔结构(000066 76→82笔) → 周线L2方向翻转
  (000066 2024-02 一买 weekly 向上→向下) → 底部一买被49课过滤(更严格, 000066/000062/
  000037 一买消失)。000062 2024-07 背驰点随笔结构变化消失(候选不再生成)

### 7.14.8 5只典型股 P0 vs v2 (2024-01~2026-08, FILTER_L2=False)
| 股票 | v2 | P0 | 变化 |
|---|---|---|---|
| 000066 | -10.2% / +81.3% (净+71.1%) | -10.0% / +86.0% (净+76.0%) | ≈(一卖力度0.00, 更早确认) |
| 000012 | 6笔 净-6.8% | +5.4/+6.0/-11.5/+19.5 (净+19.4%) | **改善**(三卖/一卖正常离场, 少做2笔) |
| 000037 | -10.9% (三买失败) | +11.8% (二买+三卖离场) | **改善** |
| 000592 | -15.2% / +362.7% (净+347%) | +3.1% / +362.7% (净+366%) | **改善**(第一笔二卖止盈, 不再破A止损) |
| 000062 | +247% (一买) | -10.4% | **变差**(07-10背驰点随笔结构消失, 一买候选不再生成) |

结论: 4/5改善或持平; 000062变差源于死锁修复的全局笔结构变化(背驰点重判定),
  需全量回归评估净效果(用户要求聚焦典型股, 全量待定)

## 7.15 审计驱动修复(2026-08-13, backtest-auditor 子agent两轮审计 P0/P1 实施)

### 7.15.1 背景
50只回测(2024-01-01~2026-08-12, 41笔)经 backtest-auditor 子agent两轮审计
(逐笔缠论审查 + 五层失败单解剖, 报告见 results/审计报告_子agent审查_v1 与
审计报告_失败单五层解剖_v2)。修复清单 6 项中 **3 项保留、3 项回滚**, 全部经全样本回归。

### 7.15.2 保留修复(3项)
1. **F1 三卖中枢相关性**(出场侧, 1行): 日K三卖中枢筛选加"持仓峰值≥中枢ZG"前提
   (peak30>=z.zg)。修复前无关高位中枢建成瞬间即"跌破ZD"误杀(000050#2/000063等5笔,
   方向随机)。回归: 5笔F1单改由④破A/⑤结构/⑥止损接管, 总账-20pt
   (41笔+449.68%→40笔+429.45%), 换取消除随机错杀。
2. **div_type 传承**(标注, 2行): 二买候选生成两处(scan30/Phase4b)复制一买源类型。
   交易记录自带 div_type。引擎自输出分组: 趋势背驰源二买21笔 48% avg+19.59% vs
   盘整背驰源6笔 33% avg+1.07% — 27课"盘整背驰不构成一买"成立。
3. **盘整背驰源入场收紧**(入场侧, 1行): 入场比上限双条件 — 盘整背驰源二买≤A×1.05,
   趋势背驰源保持A×1.10。回归: +18.4pt(→39笔 44% +447.84%), 拦下最大爆亏单
   000006#1(-14.16%, 入场/A=1.078); 000062(+236%, 入场/A=1.09)属趋势背驰源不受影响。

### 7.15.3 回滚尝试(3项, 教训)
1. **21课盘整高点回撤扩展至二买**(r2): 以"peak30<入场×1.10"为护栏仍系统误杀 —
   000062入场后头两周peak未过×1.10即被5%回撤砍出(+236%→-2.6%), 总账+429→+113。
   **教训: 任何"早点卖"规则都会系统性地杀趋势赢家。**
2. **二买56天时间闸**(r4): 一买→二买>56天不生成候选。000062/000006#2(两个最大
   赢家)间隔同样>56天, 一刀切总账+429→+120。
   **教训: 失败单侧统计特征(追尾天数)赢家同样拥有, 不能直接变成过滤规则。**
3. **三买"第一次回试"追踪**(r6): 回试完成未消费→候选作废。杀3笔失败单-9.8%但
   误杀2笔盈利单+19.5%(000050#5 +13.27), 且000001(94天追尾)因首次回试过浅未形成
   日K笔而漏网。净-9.7pt。**教训同上: 追尾特征无赢输区分度。**

### 7.15.4 回归结果总表(50只, 2024-2026)
| 版本 | 笔数 | 胜率 | 总盈亏 | 平均/笔 | 说明 |
|---|---|---|---|---|---|
| 基线 | 41 | 46% | +449.68% | +10.97% | 审计前 |
| r1 F1修复 | 40 | 42% | +429.45% | +10.74% | 随机错杀消除 |
| r3 +div_type标签 | 40 | 42% | +429.45% | +10.74% | 纯标注零行为变化 |
| **r5 +双条件收紧(最终)** | **39** | **44%** | **+447.84%** | **+11.48%** | 当前引擎 |
| r6 +三买第一次追踪 | 34 | 44% | +438.16% | +12.89% | 已回滚 |

### 7.15.5 纪律结论
- **新规则必须全样本回归验证; 只看失败单侧统计必然误伤赢家(连续3次教训)。**
- 证据文件: results/bt_d30_50_20260813.json(基线)、results/bt_d30_50_r5_cap105.json(最终)。
- 引擎内已留注释标记每处修改与回滚原因(r2/r4/r6 教训均在代码注释中)。

---

## 7.16 一买中枢来源修复与等价性能优化(2026-08-17)

### 7.16.1 C段结构边界
- 旧实现对每个历史第二中枢 `b` 使用 `bars[b.end+1:]` 搜索到当前前缀末端的最低点。
  结果是旧中枢可跨越多个后来中枢，重新绑定到数年后的低点。
- 新实现以完整 `zs_L1` 序列中 `b` 后的第一个同级中枢为硬边界：
  `C = [b.end+1, next_zs.start)`；若没有后续中枢，才延伸到当时可用数据末端。
- 硬边界使用过滤前的完整中枢序列；宽度不足0.3%的中枢虽不参与候选生成，仍能截断旧C段。
- 该边界是日K笔中枢代理的保守边界；严格缠论仍要求离开走势完成和次级别反转确认。
- 同一 `signal_date` 仍有多种结构解释时，选择 `(b.end_dt, b.start_dt)` 最近者，
  不使用扫描顺序、MACD大小或事后盈亏决定来源。
- 若更近来源在后续交易日才确认，且该信号槽尚未消费，引擎替换旧源并撤销其未消费二买，
  在同一轮按新源重建二买；已消费信号槽不允许重复入场。
- 审计 `candidate_id` 加入源中枢开始/结束及 `ZD/ZG`，保证二买可追溯到唯一源一买结构。

### 7.16.2 不改变算法的性能优化
- `FAST_DAILY_AVAIL=True` 用有序日线日期二分定位每日可用前缀，替代每天从第一根日K扫描到全量末端。
- `FAST_DAILY_MACD_PREFIX=True` 直接复用因果MACD全量数组；扫描只访问可用日K索引，
  与每日复制 `diff/dea` 前缀逐字段等价。
- `False` 保留原基线路径；真实股票回归要求两条路径的 `trades`、`candidates` 逐字段完全相等。
- 性能优化不改变候选生成、优先级、成交时点、价格、退出条件或统计口径。

---

## 7.17 正式一买与因果二买链严格化（2026-08-17）

### 7.17.1 正式一买硬门槛

日K笔构造的30分钟级别中枢仍是本项目的操作代理，但只有同时满足下列条件才标记为
正式一买：

1. A、B是过滤前原始同级中枢序列中直接相邻的两个中枢；不得跳过向上或窄中枢。
2. A、B方向均为向下，宽度均大于0.3%，并且都已有后续离开笔确认完成。
3. 两个中枢区间必须不重叠并向下：`B.ZG < A.ZD`。同时报告更严格的
   `B.GG < A.DD`完整波动隔离，但它不是108课趋势定义的硬门槛，也不得用来把策略删空。
4. C从`B.end`后的第一根日K开始，在下一同级中枢开始前截止。
5. C低点发生在B完成确认之后，且严格低于`B.DD`。
6. A、C的向下MACD面积均为负，且`abs(C面积)/C长度 < abs(A面积)/A长度 × 0.9`。
7. 只有“趋势背驰”可生成正式一买；盘整背驰、类一买仅可作为观察解释，禁止派生正式二买。

中枢对象增加成员笔索引、最初三笔边界、延伸后边界、完成状态和离开笔确认时点；
正式一买冻结两中枢、A/C段和全部硬门槛，供审计逐笔复核。

### 7.17.2 二买来源与时序

- 二买必须带唯一`source_b1_id`，且源一买的全部正式门槛为真、`div_type=趋势背驰`。
- 30分钟向下回调笔必须在源一买`first_seen_at`之后开始并确认；禁止在后来确认一买时，
  回溯使用早已发生的30分钟回调。
- 二买进入时再次校验源证据与时间顺序，防止旧缓存或历史候选绕过生成门槛。
- 审计分别记录`occur_at`、原子`confirm_at`、`structure_confirm_at`、
  `first_seen_at`、`effective_confirm_at`、`decision_at`和`fill_at`。

### 7.17.3 报告冻结口径

交互证据图以被采用候选的`first_seen_at`冻结结构，缺失时才退回`decision_at`；
`signal_at`只作为历史低点标记。盘中10:00首次可见的候选只展示前一交易日已经完成的日K，
避免把回填低点日期误当成当时已经可见的中枢结构。

### 7.17.4 日线/30分钟价格尺度绑定

数据库没有统一复权标签，个别股票存在日线未复权、30分钟前复权的稳定比例差异。
正式一买结构仍在日线自身尺度计算；当日线A价进入30分钟二买的价格区、破位和入场上限谓词时，
必须用A日的日线OHLC与30分钟聚合OHLC绑定尺度：四个比值的极差不超过0.25%才可采用
其中位比例，比例与1相差不超过0.5%时按1处理以忽略分笔/舍入噪声。无法稳定绑定时禁止派生二买，
不得把不同复权尺度的价格直接比较。审计同时保存日线A价、30分钟A价、转换比例和比值极差。
